{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "npm ci --include=dev",
  "buildCommand": "R=$(pwd); while [ ! -d \"$R/backend\" ] && [ \"$R\" != \"/\" ]; do R=$(dirname \"$R\"); done; cd \"$R\" && npm ci --include=dev && ./node_modules/.bin/prisma generate --schema backend/prisma/schema.prisma && cd frontend && npm ci --include=dev && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
